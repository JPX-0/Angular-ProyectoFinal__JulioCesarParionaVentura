import { ResolvePageNamePipe } from './resolve-page-name.pipe';

describe('ResolvePageNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ResolvePageNamePipe();
    expect(pipe).toBeTruthy();
  });
});
