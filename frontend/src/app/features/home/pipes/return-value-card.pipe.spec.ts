import { RenderCardPipe } from './render-card.pipe';

describe('RenderCardPipe', () => {
  it('create an instance', () => {
    const pipe = new RenderCardPipe();
    expect(pipe).toBeTruthy();
  });
});
