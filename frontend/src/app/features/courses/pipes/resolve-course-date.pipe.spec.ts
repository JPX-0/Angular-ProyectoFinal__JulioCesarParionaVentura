import { ResolveCourseDatePipe } from './resolve-course-date.pipe';

describe('ResolveCourseDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ResolveCourseDatePipe();
    expect(pipe).toBeTruthy();
  });
});
